spring.application.name=user
# Server port (default is 8080, but you can change if needed)
server.port=8080

# Logging level for Spring Security (helpful for debugging)
logging.level.org.springframework.security=DEBUG

# Database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/auth_rbac
spring.datasource.username=hridoy
spring.datasource.password=hridoy@123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
